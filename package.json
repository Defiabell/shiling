{
  "name": "shiling",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b packages/sim packages/content packages/client packages/tale-sim packages/tale-content packages/tale-ai packages/tale-client && tsc --noEmit -p packages/gen",
    "dev": "pnpm --filter @shiling/client dev",
    "dev:tale": "pnpm --filter @shiling/tale-client dev"
  },
  "devDependencies": { "typescript": "^5.5.0", "vitest": "^2.0.0" }
}
